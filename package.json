{
  "scripts": {
    "test": "jest --runInBand --ci"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^13.0.0",
    "supertest": "^6.0.0",
    "xmlbuilder2": "^3.1.1"
  },
  "jest": {
    "reporters": [
      "default",
      [
        "./reporters/custom-junit-reporter.js",
        {
          "outputDirectory": "test-reports",
          "outputName": "results.xml"
        }
      ]
    ]
  }
}